----------------------------------TODO Backend----------------------------
- Подсчет количества обращений(HITS) по часам            [DONE]
- Подсчет уникальных дневных IP по часам                 [DONE]
- Подсчет трафика по часам                               [DONE]
- Подсчет количества страниц по часам                    [DONE]
- Создание отображений: IP - bytes/pages/visits/hits     [DONE]
- Как-то считать визиты                                  [DONE but consider revising]
- Make some fool defence (against wrong input files)     [DELAYED]
- Считать уникальные посещения через Cookie              [DELAYED - QUESTION] 
- Для больших файлов сделать обработку по схеме:		
	есть файлы 1 - новые, 2 - обрабатываемые, 3 - обработанные логи, 4…N - получаемые файлы со статой
	При запуске stat.exe:
	1) Из 2 все добавляется в 3
	2) Из 1 все перемещается в 2
	3) Берется по одной строке из 2, обрабатывается, записывается сразу в 3
	4) Время от времени инфа добавляется в 4…N
- Параллельная обработка логов
- Подсчет в файлах статистики суммы сразу после даты     [DELAYED]
- Убрать лишние set'ы (для уникальных IP)   


----------------------------------TODO Frontend----------------------------

1) СКРИПТ POSTINSTALL
	[- Отключать статистику (awstats,webalizer)] 
	- Получить список всех подписок
		а) Создать .../var/subscriptions.txt - список подписок
		б) Создать mkdir в .../var/ - папки для всех подписок
		в) Получить для каждой подписки список вебсайтов и сохрнаить его в .../var/<subscription_name>/sites.txt
		г) Создать mkdir .../var/<subscription_name>/ - папки для всех вебсайтов

2) СКРИПТ РЕГУЛЯРНЫЙ
	[-Проверить (наличие новых/отстутствие старых) подписок/сайтов и сформировать каталог .../var/ соответсвенно]
	- Запустить /var/backend.exe (он возьмет данные из subscriptions.txt и sites.txt)
	
3) FRONTEND (index.php)
	- имеется combobox1: выбрать подписку
	- имеется combobox2: выбрать стату по определенному сайту/всей подписке
	- имеется combobox3: выбрать стату по дню/месяцу/году
		а) день
			- имеется calendar1 выбор даты
			- имеется combobox4 выбора показываемого параметра (hits/visits/unique_visitors/bandwidth)   (или все вместе?)
			- показывается по часам график изменения параметра(ов)
			- показывается столбик - топ ip с параметрами (hits/visits/unique_visitors/bandwidth)
		б) месяц
			- имеется calendar1, calendar2 для выбора диапазона дат
			- имеется combobox4 выбора показываемого параметра (hits/visits/unique_visitors/bandwidth)   (или все вместе?)
			- показывается по дням график изменения параметра(ов)
		    [- показывается столбик - топ ip с параметрами (hits/visits/unique_visitors/bandwidth)]
		в) год
			- имеется calendar1, calendar2 для выбора диапазона дат
			- имеется combobox4 выбора показываемого параметра (hits/visits/unique_visitors/bandwidth)   (или все вместе?)
			- показывается по месяцам график изменения параметра(ов)
		    [- показывается столбик - топ ip с параметрами (hits/visits/unique_visitors/bandwidth)]
	- кнопка "показать" (потом не будет)
	- берет данные из .stat файлов в /var/<subscription_name>/<site_name>
	
	
	- ОПТИМАЛЬНОСТЬ! Прочитывать весь файл, беря информацию из дат, попадающих в промежуток